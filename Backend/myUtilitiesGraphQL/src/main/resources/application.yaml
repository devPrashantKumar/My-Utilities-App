spring:
  application:
    name: myUtilitiesGraphQL


  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb

server:
  servlet:
    context-path: /gql-api