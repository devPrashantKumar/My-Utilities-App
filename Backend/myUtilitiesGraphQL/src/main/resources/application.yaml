spring:
  application:
    name: myUtilitiesGraphQL
  graphql:
    graphiql:
      enabled: true

  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb

server:
  servlet:
    context-path: /utilities-api

#voyager:
#  enabled: true
#  endpoint: /graphql

logging:
  level:
    org:
      springframework:
        graphql : DEBUG